name: Build JSDoc

on: [push]

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: andstor/jsdoc-action@v1
      with:
        source: ./scripts       # path to your code
        destination: ./scripts/docs  # output directory
